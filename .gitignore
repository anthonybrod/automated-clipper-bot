__pycache__/
*.pyc
.env
*.env
enterprise_workspace/
downloads/
*.mp4
*.mp3
*.wav
*.srt
.ipynb_checkpoints/

# Session transcripts - backed up outside the repo, never committed (66MB+)
*.jsonl

# Local session logs - user prompts and state snapshots. NEVER commit:
# this is a PUBLIC repo and these contain raw conversation content.
.claude/
